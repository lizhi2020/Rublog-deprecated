language: rust
cache: cargo
script:
- cargo build
deploy:
  provider: releases
  skip_cleanup: true
  github_token: $GITHUB_TOKEN  # Set in the settings page of your repository, as a secure variable
  keep_history: true
  on:
    branch: main
